<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <!-- Brand and toggle get grouped for better mobile display -->
  <div class="navbar-header">
    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <a class="navbar-brand" href="<%= admin_orders_path %>">JDstore 后台</a>
  </div>

  <div class="collapse navbar-collapse navbar-ex1-collapse">
    <ul class="nav navbar-nav navbar-right">
      <li class="dropdown">
        <a class="dropdown-toggle" data-toggle="dropdown" href="#">
          <i class="fa fa-user" aria-hidden="true"></i>
          <%= current_user.email %>
          <b class="caret"></b>
        </a>
        <ul class="dropdown-menu">
          <li><%= link_to(fa_icon("arrow-left", text: " 回到前台"), products_path) %></li>
          <li class="divider"></li>
          <li><%= link_to(fa_icon("sign-out", text: " 退出"), destroy_user_session_path, method: :delete) %></li>
        </ul>
      </li>
    </ul>
    <!-- Sidebar Menu Items - These collapse to the responsive navigation menu on small screens -->
    <div class="sidebar" data-color="purple">
      <div class="sidebar-wrapper">
        <ul class="nav">
          <li class="<%= render_active "orders" %>">
            <%= link_to(admin_orders_path) do %>
              <i class="material-icons">dashboard</i>
              <p>订单列表</p>
            <% end %>
          </li>
          <li class="<%= render_active "categories" %>">
            <%= link_to(admin_categories_path) do %>
              <i class="material-icons">view_column</i>
              <p>课程分类</p>
            <% end %>
          </li>
          <li class="<%= render_active "products" %>">
            <%= link_to(admin_products_path) do %>
              <i class="material-icons">view_list</i>
              <p>课程列表</p>
            <% end %>
          </li>
          <li class="<%= render_active "users" %>">
            <%= link_to(admin_users_path) do %>
              <i class="material-icons">person</i>
              <p>用户列表</p>
            <% end %>
          </li>
        </ul>
      </div>
    </div>
  </div>
  <!-- /.navbar-collapse -->
</nav>
