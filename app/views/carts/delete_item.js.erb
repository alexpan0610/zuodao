$('.cart-nav').html('<%= escape_javascript render(partial: "common/cart") %>');
$('.cart-content').html('<%= escape_javascript render "carts/cart_content" %>');
$('#product-quantity').html('<%= @product.quantity %>');
setSelections(<%= @selections.to_json.html_safe %>);
listenCartItemsSelections();
if (<%= @product.quantity %> > 1) {
  $('#quantity-plus').removeClass("disabled");
  $('.btn-product-option').removeAttr("disabled");
  $('.btn-product-option').removeClass("disabled");
}
