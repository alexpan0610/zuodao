<div class="my-content product-list">
  <div class="container">
    <div class="row">
      <% if @products.empty? %>
        <%= render "empty" %>
      <% else %>
        <%= render partial: "product", collection: @products, as: :product %>
      <% end %>
    </div>
  </div>
</div>
