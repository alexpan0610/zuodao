$('.cart-nav').html('<%= escape_javascript render(partial: "common/cart") %>')
$('#product-quantity').html('<%= @product.quantity == 0 ? "已报满" : @product.quantity %>')
$("#quantity-minus").addClass("disabled");
$('#quantity-input').val('1')
if(<%= @product.quantity %> < 1 ){
  $('#quantity-plus').addClass("disabled");
  $('.btn-product-option').attr("disabled");
  $('.btn-product-option').addClass("disabled");
}
